#include <stdio.h>
void main(void)
{
    int size,i,value,position;
    int arr[30];
    int low,high;
    printf("Enter array size : ");
    scanf("%d",&size);
    for(i=0;i<size;i++)
    {
        printf("Enter the Integers : ");
        scanf("%d",&arr[i]);
    }
}
